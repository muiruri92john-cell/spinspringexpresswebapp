<%- include('../layouts/main') %>
<style>
.admin-nav { background: #1a1a2e; border-bottom: 2px solid #00d4ff; padding: 15px 0; }
.admin-nav a { color: #e0e0e0; text-decoration: none; margin: 0 15px; }
body { background: #0f0f1a; }
.card-dark { background: #1a1a2e; border: 1px solid #2a2a4a; border-radius: 15px; padding: 20px; margin-bottom: 20px; }
</style>
</head>
<body style="background:#0f0f1a;">

<nav class="admin-nav">
  <div class="container">
    <a href="/admin">← Back to Admin</a>
  </div>
</nav>

<div class="container py-4">
  <h2 style="color:#fff;"><%= device.device_name %></h2>
  <p style="color:#888;">Device ID: <%= device.device_id %> | Status: <span style="color:<%= device.status === 'online' ? '#10b981' : '#ef4444' %>;"><%= device.status %></span></p>

  <div class="card-dark">
    <h4 style="color:#00d4ff;">Send Command</h4>
    <form action="/admin/device/<%= device.device_id %>/command" method="POST" style="display:flex;gap:10px;flex-wrap:wrap;">
      <select name="command_type" style="background:#0a0a16;color:#fff;border:1px solid #2a2a4a;padding:10px;border-radius:8px;">
        <option value="change_price">Change Price</option>
        <option value="add_games">Add Games</option>
        <option value="restart">Restart Device</option>
      </select>
      <input type="text" name="command_value" placeholder="Value" required style="background:#0a0a16;color:#fff;border:1px solid #2a2a4a;padding:10px;border-radius:8px;">
      <button type="submit" style="background:#00d4ff;color:#000;border:none;padding:10px 25px;border-radius:8px;font-weight:700;">Send</button>
    </form>
  </div>

  <div class="card-dark" style="display:flex;gap:10px;">
    <form action="/admin/device/<%= device.device_id %>/reset-revenue" method="POST" onsubmit="return confirm('Reset revenue?');">
      <button type="submit" style="background:#f59e0b;color:#000;border:none;padding:10px 20px;border-radius:8px;">Reset Revenue</button>
    </form>
    <form action="/admin/device/<%= device.device_id %>/delete" method="POST" onsubmit="return confirm('DELETE?');">
      <button type="submit" style="background:#ef4444;color:#fff;border:none;padding:10px 20px;border-radius:8px;">Delete Device</button>
    </form>
  </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>